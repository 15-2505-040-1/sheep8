<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cloud.sheep2">

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DifActivity" />
        <activity android:name=".SubjectActivity" />
        <activity android:name=".EngdifActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".BokuActivity" />
        <activity android:name=".ResultActivity" />
        <activity
            android:name=".DatabeseActivity"
            android:label="@string/title_activity_item"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ItemEditActivity"
            android:label="@string/title_activity_item_edit"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CanvasView" />
        <activity android:name=".SampleSurfaceHolderCallback" />
    </application>

</manifest>